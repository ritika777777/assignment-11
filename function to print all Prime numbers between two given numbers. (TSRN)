#include<stdio.h>
void prime(int,int);
int main()
{
int num1,num;
printf("enter two number");
scanf("%d%d",&num1,&num);
prime(num1,num);
return 0;
}
void prime(int num1,int num)
{
    int i;
  for(i=num1;i<num;i++){
    if(i%2==1 || i==2)
        printf("%d",i);
 }
}
